##########################
### Project definition ###
##########################
cmake_minimum_required(VERSION 3.2)

set(project_name  "SOMIP_Decoder")
project(${project_name})



set(SOMIP_PROJECT_ROOT_PATH "${CMAKE_CURRENT_LIST_DIR}/..")

#include custom specific
set(SOURCES "${SOMIP_PROJECT_ROOT_PATH}/SOMIPDecoder.cpp")
list(APPEND SOURCES "${SOMIP_PROJECT_ROOT_PATH}/SOMIPDecoder.h")
list(APPEND SOURCES "${SOMIP_PROJECT_ROOT_PATH}/SomeIpVersionFactoryBmwSrr5.h")
list(APPEND SOURCES "${SOMIP_PROJECT_ROOT_PATH}/ISOMEIPDecoderAPI.h")
list(APPEND SOURCES "${SOMIP_PROJECT_ROOT_PATH}/SOMIP_Base.h")
list(APPEND SOURCES "${SOMIP_PROJECT_ROOT_PATH}/SOMIPCommonHeader.h")

list(APPEND SOURCES "${SOMIP_PROJECT_ROOT_PATH}/SOMIPHeader_A310.h")
list(APPEND SOURCES "${SOMIP_PROJECT_ROOT_PATH}/SOMIPHeader_A370.h")
list(APPEND SOURCES "${SOMIP_PROJECT_ROOT_PATH}/SOMIPHeader_A350.h")
list(APPEND SOURCES "${SOMIP_PROJECT_ROOT_PATH}/SOMIPHeader_A390.h")
list(APPEND SOURCES "${SOMIP_PROJECT_ROOT_PATH}/SOMIPHeader_A410.h")
list(APPEND SOURCES "${SOMIP_PROJECT_ROOT_PATH}/SOMIPHeader_A430.h")
list(APPEND SOURCES "${SOMIP_PROJECT_ROOT_PATH}/SOMIPHeader_A480.h")

list(APPEND SOURCES "${SOMIP_PROJECT_ROOT_PATH}/CommonHeaders.h")

list(APPEND SOURCES "${SOMIP_PROJECT_ROOT_PATH}/SOMIPDecoderAPI.cpp")

list(APPEND SOURCES "${SOMIP_PROJECT_ROOT_PATH}/SOMIP_Base_A310.h")
list(APPEND SOURCES "${SOMIP_PROJECT_ROOT_PATH}/SOMIP_Base_A310.cpp")

list(APPEND SOURCES "${SOMIP_PROJECT_ROOT_PATH}/SOMIP_Base_A370.h")
list(APPEND SOURCES "${SOMIP_PROJECT_ROOT_PATH}/SOMIP_Base_A370.cpp")

list(APPEND SOURCES "${SOMIP_PROJECT_ROOT_PATH}/SOMIP_Base_A350.h")
list(APPEND SOURCES "${SOMIP_PROJECT_ROOT_PATH}/SOMIP_Base_A350.cpp")

list(APPEND SOURCES "${SOMIP_PROJECT_ROOT_PATH}/SOMIP_Base_A390.h")
list(APPEND SOURCES "${SOMIP_PROJECT_ROOT_PATH}/SOMIP_Base_A390.cpp")

list(APPEND SOURCES "${SOMIP_PROJECT_ROOT_PATH}/SOMIP_Base_A410.h")
list(APPEND SOURCES "${SOMIP_PROJECT_ROOT_PATH}/SOMIP_Base_A410.cpp")

list(APPEND SOURCES "${SOMIP_PROJECT_ROOT_PATH}/somipBaseA430.h")
list(APPEND SOURCES "${SOMIP_PROJECT_ROOT_PATH}/somipBaseA430.cpp")

list(APPEND SOURCES "${SOMIP_PROJECT_ROOT_PATH}/somipBaseA480.h")
list(APPEND SOURCES "${SOMIP_PROJECT_ROOT_PATH}/somipBaseA480.cpp")

list(APPEND SOURCES "${SOMIP_PROJECT_ROOT_PATH}/Resource.rc")
list(APPEND SOURCES "${SOMIP_PROJECT_ROOT_PATH}/ReleaseNotes_SOMIP.txt")
list(APPEND SOURCES "${SOMIP_PROJECT_ROOT_PATH}/Version.h")


set(target_name ${project_name})
add_library(${target_name} SHARED "")

target_include_directories(${target_name} PRIVATE ${COMMON_STRUCTURES})

target_sources(${target_name} PRIVATE ${SOURCES})
if(OUTPUT_PATH)
set_target_properties(${target_name} PROPERTIES LIBRARY_OUTPUT_DIRECTORY "${OUTPUT_PATH}"
												RUNTIME_OUTPUT_DIRECTORY "${OUTPUT_PATH}"
												ARCHIVE_OUTPUT_DIRECTORY "${OUTPUT_PATH}") 				
endif(OUTPUT_PATH)




if(MSVC)
	target_compile_definitions(${target_name} PRIVATE  _WIN_PLATFORM) 
endif(MSVC)

