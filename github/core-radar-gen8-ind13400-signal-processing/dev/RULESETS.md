# Repository Rulesets

🔗 **[View Rulesets in Repository Settings](https://aptv.ghe.com/GPO/core-radar-gen8-ind13400-signal-processing/rules)**

This document describes the rulesets applied to the **GPO/core-radar-gen8-ind13400-signal-processing** repository.

These rulesets enforce governance policies and protect critical branches and tags from unauthorized modifications.

---

## Active Rulesets (7)

### Codex Validation

**Status:** `active`

**Applies to:** Branches

**Protected References:**
- `~DEFAULT_BRANCH`

**Rules Applied:**


---

### Deletions/transfers restriction

**Status:** `active`

**Applies to:** Repositoryes

**Rules Applied:**


---

### Visibility Restriction

**Status:** `active`

**Applies to:** Repositoryes

**Rules Applied:**


---

### Organization-Level Governance Ruleset - Branch

**Status:** `active`

**Applies to:** Branches

**Protected References:**
- `~DEFAULT_BRANCH`
- `refs/heads/main`
- `refs/heads/includes/**`

**Rules Applied:**

- 🚫 **Prevent deletion** - Cannot delete protected references
- 🚫 **Prevent force pushes** - Disallows force push and history rewrites

**Bypass Permissions:**

The following actors can bypass these rules:

- **Repository Role** ID: 11 (bypass mode: `always`)
- **Integration** (bypass mode: `always`)
- **BusinessTeam** (bypass mode: `always`)

---

### Organization-Level Governance Ruleset - Tags

**Status:** `active`

**Applies to:** Tages

**Protected References:**
- `~ALL`

**Rules Applied:**

- 🚫 **Prevent deletion** - Cannot delete protected references
- 🚫 **Prevent force pushes** - Disallows force push and history rewrites
- 🚫 **Prevent creation** - Cannot create new references matching the pattern
- 🚫 **Prevent updates** - Cannot update existing references

**Bypass Permissions:**

The following actors can bypass these rules:

- **Repository Role** ID: 11 (bypass mode: `always`)
- **Repository Role** ID: 21 (bypass mode: `always`)
- **Integration** (bypass mode: `always`)
- **BusinessTeam** (bypass mode: `always`)

---

### Repo-Level Standard Governance Ruleset - Branch

**Status:** `active`

**Applies to:** Branches

**Protected References:**
- `refs/heads/main`
- `refs/heads/release/*`

**Rules Applied:**

- 🚫 **Prevent deletion** - Cannot delete protected references
- 🚫 **Prevent force pushes** - Disallows force push and history rewrites
- ✅ **Required status checks** - Must pass before merging:
  - `build-analyze`
  - `unit-tests`

**Bypass Permissions:**

The following actors can bypass these rules:

- **Organization Admins** (bypass mode: `always`)

---

### Repo-Level Standard Governance Ruleset - Tags

**Status:** `active`

**Applies to:** Tages

**Protected References:**
- `~ALL`

**Rules Applied:**

- 🚫 **Prevent deletion** - Cannot delete protected references
- 🚫 **Prevent force pushes** - Disallows force push and history rewrites
- 🚫 **Prevent creation** - Cannot create new references matching the pattern
- 🚫 **Prevent updates** - Cannot update existing references

**Bypass Permissions:**

The following actors can bypass these rules:

- **Organization Admins** (bypass mode: `always`)
- **Repository Role** ID: 21 (bypass mode: `always`)

---

## Managing Rulesets

### How to View Rulesets

1. Navigate to [Repository Settings](https://aptv.ghe.com/GPO/core-radar-gen8-ind13400-signal-processing/settings)
2. Go to **Rules** → **Rulesets**
3. View and manage existing rulesets

### How to Modify Rulesets

Repository rulesets can be modified by:
- Organization administrators
- Users with repository admin permissions

### Important Notes

- Rulesets are enforced at the Git reference level (branches and tags)
- Organization admins can bypass most ruleset restrictions
- Changes to rulesets take effect immediately
- Rulesets are evaluated in addition to branch protection rules

---

_This documentation was automatically generated by the Repository Migration Service._
